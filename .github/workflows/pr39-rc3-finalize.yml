name: PR39 RC3 Finalize

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'PR39 RC3 finalizer is paused while the complete verified archive is staged.'
