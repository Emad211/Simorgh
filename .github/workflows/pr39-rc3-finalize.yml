name: PR39 RC3 Finalizer Paused for Ruff Diagnosis

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RC3 apply and targeted regressions passed; Ruff diagnosis is isolated next.'
