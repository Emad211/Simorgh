name: PR39 RC3 Finalizer Paused for MyPy Diagnosis

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Ruff passed; exact MyPy diagnostics are isolated next.'
